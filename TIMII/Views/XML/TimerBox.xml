<!-- TimerBox - A rectangle box view for the timer -->
<UIView
    backgroundColor="isTimerRunning ? UIColor.porcelain : UIColor.camo"
    height="200"
    width="100% - 20"
    layer.cornerRadius="15"
    layer.shadowRadius="2.0"
    layer.shadowOpacity="1"
    layer.shadowColor="#AAA"
    layer.shadowOffset.width="3.0"
    layer.shadowOffset.height="3.0">
    
    <!-- Timer Name -->
    <UITextField
        outlet="timerNameTextField"
        placeholder="enter task name"
        text="{name}"
        touchDown="timerNameTextFieldTouchDown"
        width="100%"
        top="height / 2 + 5"
        textAlignment="center"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next 18"
    />
    
    <!-- Daily Total Label -->
    <!-- 8.5 - removed for now until working
    <UILabel
        text="Today:1h1m"
        width="100%"
        top="previous.bottom"
        textAlignment="center"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next 10"
    />
    -->
    
    <!-- Minute -->
    <UILabel
        text="{minute}"
        top="50% - height/2 + 5"
        left="33% - width/2"
        textAlignment="center"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next ultraLight 70"
    />
    <UILabel
        text="m"
        top="50% - height/2 + 22"
        left="previous.right - 2"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next 16"
    />
    
    <!-- : -->
    <UILabel
        text=":"
        top="50% - height/2 - 2"
        left="50% - width/2"
        textAlignment="center"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next ultraLight 70"
    />

    <!-- Second -->
    <UILabel
        text="{second}"
        top="50% - height/2 + 5"
        right="33% - width/2"
        textAlignment="center"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next ultraLight 70"
    />
    <UILabel
        text="s"
        top="50% - height/2 + 22"
        left="previous.right - 2"
        textColor="isTimerRunning ? UIColor.black : UIColor.white"
        font="Avenir Next 16"
    />
    
    <!-- START / PAUSE BUTTON -->
    <UIButton
        title="{isTimerRunning ? 'Pause':'Start'}"
        touchUpInside="{isTimerRunning ? 'pauseTimer':'startTimer'}"
        backgroundColor="{isTimerRunning ? parent.backgroundColor : UIColor.ironOpaque}"
        layer.cornerRadius="height/2"
        bottom="{isTimerRunning ? 10 : 50% - height/2}"
        left="50% - width/2"
        width="height"
        height="{isTimerRunning ? 50 : 55% }"
        titleColor="isTimerRunning ? UIColor.black : UIColor.black"
    />
    
    <UIButton
        title="Reset"
        touchUpInside="resetTimer"
        bottom="10"
        right="20"
        titleColor="UIColor.white"
        isHidden="isTimerRunning ? true : false"
    />
        
</UIView>

