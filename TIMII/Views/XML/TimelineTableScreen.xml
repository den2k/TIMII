<!-- TimelineTableScreen.xml -->

<!--
 9.20.18 - Because the outlet reference in an externalized cell XML file seems buggy in for uitableviewcells, I'm now trying to pull all of the code from TimelineCell.xml into the tableviewcontroller.xml file. Wish me luck. Tried it and it still does not work.
 -->

 <TimelineTableScreen
    view.backgroundColor="gray // this defines the background of the entire tableView">
    
    <UITableView
        outlet="timelineTableView"
        backgroundColor="// use view.backgroundColor to set color. not here."
        contentInset.bottom="safeAreaInsets.bottom"
        contentInset.top="safeAreaInsets.top"
        contentInsetAdjustmentBehavior="never"
        contentOffset.y="-safeAreaInsets.top    // this creates the offset for the iPhone notch. Doesn't work when tabbing in"
        estimatedSectionHeaderHeight=""
        scrollIndicatorInsets.bottom="safeAreaInsets.bottom"
        scrollIndicatorInsets.top="safeAreaInsets.top"
        separatorStyle="none    // removes the separator line between cells"
        style="//plain"
        >

    </UITableView>
    
</TimelineTableScreen>
